import 'package:flutter/material.dart';

class ProgramingSection extends StatelessWidget {
  const ProgramingSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ProgramingSection'));
  }
}